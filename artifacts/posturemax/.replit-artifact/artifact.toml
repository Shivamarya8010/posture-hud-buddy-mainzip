kind = "web"
previewPath = "/"
title = "PostureMax"
version = "1.0.0"
id = "artifacts/posturemax"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24859

[services.development]
run = "pnpm --filter @workspace/posturemax run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/posturemax", "run", "build" ]
publicDir = "artifacts/posturemax/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24859"
BASE_PATH = "/"
